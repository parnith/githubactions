name: upper-build

on:
  push: 
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run:  pwd && ls -al
      - uses: actions/checkout@v6
      - run:  pwd && ls -al
      - name: Run script
        run: echo Hello World!!
      - run: go version
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - run: go version     